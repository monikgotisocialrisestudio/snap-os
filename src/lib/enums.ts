export enum DASHBOARD_TABS {
  ACTIVE_RUNS = "active-runs",
  EXECUTE = "execute",
  PRESETS = "presets",
}

export enum STEPS {
  MODEL = "Model",
  SCHEDULE = "Run Schedule",
  DEVICE = "Select Devices",
}

